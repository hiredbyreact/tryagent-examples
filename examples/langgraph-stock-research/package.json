{
  "name": "@tryagent/langgraph-stock-research-example",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test:e2e": "corepack pnpm run build && node --test test/*.e2e.mjs",
    "test": "pnpm run build && node --test test/*.test.mjs",
    "start": "pnpm run build && node dist/cli.js"
  },
  "dependencies": {
    "@langchain/langgraph": "^1.4.2",
    "@tryagent/sdk": "^0.0.57"
  },
  "devDependencies": {
    "@types/node": "^22.10.2"
  }
}
