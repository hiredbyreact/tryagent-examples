# Workflow Escalation Catalog

Runnable TryAgent escalation examples for common human-in-the-loop workflows.

Each scenario is intentionally small: it builds the public escalation payload an AI workflow would send when it reaches an ambiguous decision, then the E2E test drives that payload through the TryAgent lifecycle.
